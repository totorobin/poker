{
  "name": "poker",
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "test:e2e": "start-server-and-test 'pnpm run dev' http://localhost:5173 'pnpm --filter @poker/e2e test'",
    "test:e2e:report": "pnpm --filter @poker/e2e report",
    "start": "pnpm --filter=@poker/server start --PUBLIC_PATH ../../client/dist --PORT 8080",
    "prestart": "pnpm -r build"
  },
  "packageManager": "pnpm@10.5.2",
  "devDependencies": {
    "start-server-and-test": "^3.0.2"
  }
}
